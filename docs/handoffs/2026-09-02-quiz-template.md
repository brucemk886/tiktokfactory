# 测试题中视频模板

日期：2026-09-02
分支：`feature/mid-video`

## 完成内容

- 中视频模板工作台新增 `/quiz`，侧栏名称为“测试题”。
- 页面支持中英文示例题库、6–9 道三选一题目、正确答案、代码插图、标题、开头钩子、结尾互动、每题停留时长和随机种子编辑。
- 新增独立 Remotion 工程 `quiz-video-generator`。成片为 720×1280、30 FPS 的试卷式动画：同屏保留 4–5 题，真实手部红笔用约 1 秒划线后立即离场，随后独立显示 5→4→3→2→1，倒计时结束用绿色勾揭晓答案，只有新题接近画面底部时才快速下滚。
- 题目标题根据中英文视觉长度自动缩放并保持单行，红色划线始终位于文字下方，不再穿过换行标题。
- 内置原创划线摩擦声、每秒倒计时节拍和答案揭晓提示音；没有背景音乐时保留这些交互音效并照常出片。
- 本地 `/api/quiz/start` 与 `/api/quiz/progress/:id` 已接入；Cloudflare 同名接口会入 `quiz` 队列，本地工人通过 `quiz-render-job.js` 执行。
- 现有管理员账号通过本地版本 27 迁移和云端动态补全获得该模块；运营账号默认不开放。
- 若内置背景音乐不存在，渲染器自动关闭音乐并继续出片，不会因缺音频失败。
- 云端任务结果保留测试题成片文件名、语言、题数、时长和种子；线上页明确提示成片保存于本地工人输出目录。

## 验证

- 内容门槛：`score_content.py --threshold 85 --strict`，100/100。
- 第四版样片：61.056 秒，H.264 720×1280 30 FPS，AAC 48 kHz 双声道；`ffmpeg` 全片解码无错误。
- 成片关键帧：1.4、2.8、7.8、32、50、60 秒共 6 帧，已检查长标题单行适配、缓速划线、手部离场、无手倒计时、答案揭晓、中段和结尾画面。
- 第一至三版基线样片已废弃，由 `quiz-template-v4-qa.mp4` 替换。
- 聚焦测试 36/36：内容规范化、模块接线、侧栏、账号迁移和 Cloudflare 任务结果均通过；相关脚本语法与 `git diff --check` 通过。

## 注意事项

- 第四版保留在 `feature/mid-video`；本次未部署、未推送，需要合并进 `main` 后由主工作树执行上线流程。
- `quiz-video-generator/output/` 下的 QA 文件匹配根目录 `*.mp4` 忽略规则，不进入 Git。
- 独立工作树验收时临时链接了主工作树的 `node_modules`；验收结束后应移除该目录链接。
