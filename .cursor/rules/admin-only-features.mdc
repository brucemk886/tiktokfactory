---
description: 新功能和额外模板只给管理员，成员已有入口不要动
alwaysApply: true
---

# 权限：新功能只给 admin

后续新页面、接口、侧栏、按钮默认只给 `admin`。不要把 `operator` / 成员加进新功能，除非用户明确要求。

本地成员已有入口保持原样：GeeLark 自动发布、数据总览、发布记录。官方「Reddit 混剪」、混剪小说书单、模板2 都只给管理员。不要再把官方通道页加给成员。
